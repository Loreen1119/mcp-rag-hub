# P0 演练手册：让 Ollama 真正跑起来 + 四类端到端验收

> 建立于 2026-09-13。目的：把「A 组（P0）」从"能跑"变成"跑过、有数字"。
> 所有命令都在项目根 `D:\1base\computer\Agent\DevRoot\mcp-rag-hub` 下执行。

---

## 0. 一句话结论

**面试现场只需要 `qwen2.5:3b`（生成）**；`qwen2.5:7b` 只在"跑评测报告"时当裁判用，**可以不做**。
所以：**演示 = 3b 就够 · 报告 = 才需要 7b**。别把两件事绑在一起。

---

## 1. 内存预算（2026-09-13 23:00 实测）

| 指标 | 实测值 |
|---|---|
| 物理内存 | 总 15.71 GB / **可用 2.14 GB** |
| 提交量（Commit） | 上限 31.43 GB / 已用 25.80 GB / **剩余 5.63 GB** |

> Ollama 的判断依据是**物理可用内存**（报错原文：`model requires more system memory (5.6 GiB) than is available (3.0 GiB)`）。

| 要跑的东西 | 需要物理可用 | 现在够吗 | 怎么办 |
|---|---|---|---|
| **3b（生成 / 演示）** | ≳ 2.5 GB | ⚠️ 卡线 | 关掉 QQ（-1.1 GB）→ 约 3.2 GB ✅ |
| **7b（裁判 / 评测）** | ≳ 6 GB | ❌ 不够 | 见下面清单，凑到 7 GB 以上 |

### 要关掉的（按性价比排序，合计约 5.2 GB）

| 进程 | 占用 | 说明 |
|---|---|---|
| chrome（28 个进程） | 2,551 MB | 最划算，先关这个 |
| QQ（8 个） | 1,118 MB | 关 |
| WeChatAppEx + Weixin | 807 MB | 关 |
| claude（4 个） | 766 MB | VS Code 里的 Claude Code 会话进程，演示时不需要 |
| Code（20 个） | 2,398 MB | 15 GB 内存的机器上，**开着 VS Code 跑 7b 会很难** |

⚠️ **WorkBuddy 自己占 3,497 MB，关不掉**（我就在里面跑）。所以算预算时这 3.5 GB 是固定支出。

### 建议的重启顺序（你原计划的重启 + 清界面）

```
1. 重启（彻底清空）
2. 只开 WorkBuddy（打开这份手册）
3. 先查内存 —— 一条命令：
   $os=Get-CimInstance Win32_OperatingSystem; "物理可用 {0:N2} GB | 提交剩余 {1:N2} GB" -f ([int64]$os.FreePhysicalMemory*1KB/1GB), ($os.FreeVirtualMemory/1MB)
4. 物理可用 ≥ 3 GB → 起 Ollama，跑 3b（演示路径）
   物理可用 ≥ 6.5 GB → 也可以跑 7b 裁判
5. 全程别开 Chrome / VS Code / QQ
```

---

## 2. 起 Ollama（两种方式，选一种）

**环境变量已经是对的**，不用管：用户级 `OLLAMA_MODELS = D:\1software\ollama_models`（已实测确认，8 个 blob 在里面）。

### 方式 A：开个窗口手动起（**演练/演示推荐**）

开一个新的 **PowerShell 窗口**，执行，然后**别关这个窗口**：

```powershell
& "D:\1software\ollama\ollama-windows-amd64\ollama.exe" serve
```

### 方式 B：无窗口后台起（脚本化/自动化用）

```powershell
Start-Process -WindowStyle Hidden -FilePath "D:\1software\ollama\ollama-windows-amd64\ollama.exe" -ArgumentList "serve"
```

### 验证（**必须跨一次调用再验**）

```powershell
# 1) 端口活着 + 模型列表正确
(Invoke-RestMethod http://127.0.0.1:11434/api/tags).models | Select-Object name, @{n='GB';e={[math]::Round($_.size/1GB,2)}}
```
期望看到 `qwen2.5:3b`（约 1.93 GB）和 `qwen2.5:7b`（约 4.36 GB）。

```powershell
# 2) 真的能推理（首次会加载模型，慢；这一步是"真热了"的证据）
& "D:\1software\ollama\ollama-windows-amd64\ollama.exe" run qwen2.5:3b "只回复 OK 两个字"
```

---

## 3. 演练五步（每步都有"看到什么算成功"）

> 所有 Python 命令前都要设环境变量（不设会联网挂住）：
> ```powershell
> $env:HF_HUB_OFFLINE="1"; $env:TRANSFORMERS_OFFLINE="1"; $env:PYTHONIOENCODING="utf-8"
> ```

### Step 1 · 环境自检（约 1 分钟，不依赖 Ollama）

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe scripts\validate_golden.py "$env:TEMP\vg.txt"; Get-Content "$env:TEMP\vg.txt"
```
- 期望：`35 passed`
- 期望：三套验收集 `问题数 0`

### Step 2 · 连通性冒烟（约 3~5 分钟，只跑 3 条）

```powershell
.\.venv\Scripts\python.exe -m src.evaluation.llm_eval --quick
```
- 脚本会先发一句"回复 OK"，**通了才继续**；不通会直接打印三条排查提示。
- 期望：3 条都产出 `answer` 字段 + 三个分数。

### Step 3 · 主验收：18 条可答（**这是 A2 的核心**）

```powershell
# ⚠️ 先备份！见第 4 节第 1 条坑
Copy-Item experiments\llm_evaluation_results.json "$env:TEMP\llm_eval_backup_20260913.json" -Force
.\.venv\Scripts\python.exe -m src.evaluation.llm_eval
```
- 产出：`experiments/llm_evaluation_results.json`（生成 3b / 裁判 7b，会记录 `judge_model` 字段）
- 记录：答案生成率、Faithfulness / Answer Relevancy / Context Recall 三项均分
- 耗时估算：3b 生成实测 **14.5~43.8 秒/条**，而每条还要 3 次裁判调用 →
  **3b 生成 + 3b 裁判 ≈ 20~40 分钟；3b 生成 + 7b 裁判 ≈ 60~90 分钟**（7b 慢 2~3 倍）。
- **建议**：先跑 `--sample 3` 拿到"3 条样例 + 一条真数字"，全量放最后。

### Step 4 · 延迟重测（`experiments/latency_profile.json` 还是 7/27 的）

```powershell
.\.venv\Scripts\python.exe -m src.evaluation.experiments --quick
```
- 产出：`experiments/latency_profile.json` 等（该文件是**被 git 跟踪**的，改坏了能还原）
- 注意：完整模式会连跑 5 个实验（消融 / 分类 / 参数扫描 / 延迟 / 深挖），CE 在 CPU 上是瓶颈，
  **检索评测单独跑就要 8~10 分钟**，全套预计 40 分钟以上 → 演练先用 `--quick`。

### Step 5 · 演示动线（约 10 分钟，**这条才是面试真正要演的**）

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py --server.headless true --server.port 8501
```
浏览器开 http://localhost:8501 ，按这个顺序走一遍：

| 顺序 | 状态 | 操作 | 看到什么算对 |
|---|---|---|---|
| 1 | `answered` | 问答模式问「**路径操作装饰器**」 | 答案带 `[1][2][3]` 引用编号 + 下方证据卡片标"已引用" |
| 2 | `answered`（跨文档） | 问「**如何用依赖注入配合 OAuth2 密码流，做一个只有登录用户才能访问的接口？**」 | 引用覆盖 ≥2 个来源 |
| 3 | `insufficient_evidence` | 问「**FastAPI 背后的公司去年营收是多少？**」 | 拒答 + 说明原因，**不能编** |
| 4 | `generation_unavailable` | **停掉 Ollama**，再问第 1 题 | 提示"回答生成服务暂不可用" + 仍展示 BM25 原文拼接 |

> 第 4 步别忘**再把 Ollama 起回来**，否则后面全废。

---

## 4. ⚠️ 三个会毁掉数据的坑（这个项目已经栽过两次）

### 坑 1：`llm_eval.py` 的产物**被 .gitignore 忽略，没有 git 恢复通道**

`.gitignore` 第 11 行是 `experiments/*`，只有 `latency_profile.json` 和 `kg_diag_report_control.json` 被 `!` 例外放行。
也就是说 `experiments/llm_evaluation_results.json`（9/12 那份**旧语料**的实测证据）**不在版本控制里**，
而 `--sample 3` / `--quick` **会直接覆盖它**。

→ **每次跑评测前先 `Copy-Item` 备份到 `$TEMP`。**（恢复通道只剩 WorkBuddy 的 `file-history`，而那需要我当时在场。）

### 坑 2：**不要** `nohup ./ollama serve &`

工具调用结束时会把该会话的子进程一起回收 —— 当次能看到 `LISTENING`，**下一次调用就没了**。
（这就是"我明明起过，怎么又没了"的真相。）用第 2 节的方式 A 或 B。

### 坑 3：**不要覆盖 `OLLAMA_MODELS`**

覆盖成 `D:\ollama_models` 或 `C:\Users\REDMI\.ollama\models` 会命中**空壳目录**（0 个 blob），
症状是「**服务在跑、端口通，但 `/api/tags` 返回 `{"models":[]}`、生成全报 `connection_error`**」。
现在用户级变量是对的，**什么都别设**就行。

---

## 5. 四类验收的现状（重要：有两类没有脚本）

| 验收 | 有自动化入口吗 | 怎么做 |
|---|---|---|
| 18 条可答 · 生成率 / 误拒率 | ✅ `llm_eval.py` | 直接跑 Step 3 |
| 10 条拒答 · 拒答率 / 幻觉率 | ❌ **没有脚本** | 只能照第 3 节 Step 5 的表**手动逐条试**（或用 `agent.py --query "..."` 单条跑） |
| 3 条多证据 · 引用覆盖率 | ❌ **没有脚本** | 同上，手动试 ME01~ME03 |
| Ollama 不可用 · 降级路径 | ✅ 无需脚本 | 停服务再问一句（Step 5 第 4 行） |

> 唯一碰过这两个集的是 `scripts/validate_golden.py`（**只查可考证性，不调 LLM**）和
> `phase0_answerability_analysis*.py`（**只算检索分数，不生成答案**）。
> 想让这 13 条也有数字，得写一个小脚本 —— **这是我下一步可以做的活，不用你动手。**

### 手动测试用的真实问题（已从验收集里取好）

**有答案（∩ 新语料）**
- `路径操作装饰器`（E02）
- `依赖注入`（E06）
- `怎么把配置项放在代码之外、运行时再读进程序，并保证类型正确？`（S06）

**应拒答**
- `FastAPI 背后的公司去年营收是多少？`（U01）
- `Vue 3 的 Composition API 里 ref 和 reactive 有什么区别？`（U04 — 语料里只把 Vue 当名字罗列，**最难的一条**）
- `本系统在生产环境承诺的 SLA 和可用性指标是多少？`（U09）

**应跨文档引用**
- `如何接收客户端上传的文件，并在响应返回之后用后台任务记录一条处理日志？`（ME03 — 跨 `request-files.md` + `background-tasks.md`）

**单条命令行探针**（不开 Streamlit，最快）：
```powershell
.\.venv\Scripts\python.exe agent.py --query "路径操作装饰器"
```

---

## 6. 故障对照表

| 症状 | 真因 | 处置 |
|---|---|---|
| `/api/tags` 返回 `{"models":[]}` | `OLLAMA_MODELS` 被覆盖成空壳目录 | 什么都别设，重启 serve |
| 生成全报 `connection_error` | 服务没起 / 被回收了 | 用方式 A 重起，跨调用再验一次 |
| 报 `model requires more system memory` | 物理可用不够 | 按第 1 节关进程 |
| 单条生成 > 60 秒 | 内存紧张导致换页，或用了 7b | 关掉 Chrome/VS Code；确认 `LLM_MODEL=qwen2.5:3b` |
| 结果里出现 `malformed_json` | 小模型没吐合法 JSON | 已接 `format="json"`；偶发属正常，看比例 |
| `Failed to send telemetry event ... capture()` | chromadb 版本兼容噪音 | **无视**，不影响数据 |
| 评测跑到一半想停 | 有 checkpoint 机制 | 直接 Ctrl+C，产物里已有部分结果 |
| 卡住十分钟没输出 | 忘了设 `HF_HUB_OFFLINE=1` | 停掉重跑，命令前先设环境变量 |

---

## 7. 时间预算（一次完整演练）

| 阶段 | 时间 |
|---|---|
| 重启 + 只开 WorkBuddy + 起 Ollama + 验证 | 5 分钟 |
| Step 1 环境自检 | 1 分钟 |
| Step 2 冒烟（`--quick`，3 条） | 3~5 分钟 |
| Step 3 主验收（18 条 · 3b 生成 + **3b 裁判**） | 20~40 分钟 |
| Step 3+ 裁判升级成 7b（可选，可只跑 3 条） | +40 分钟 |
| Step 4 延迟（`--quick`） | 10~20 分钟 |
| Step 5 演示动线（四个状态） | 10 分钟 |
| **合计（把 7b 放最后、可选）** | **约 1 小时** |

**建议的第一次演练目标**：Step 1 + Step 2 + Step 5。**先把"演示动线"走顺**，再花时间刷全量数字 ——
面试真正要演的是 Step 5，而它只需要 3b。

---

## 8. 相关文件

- 未完成项全量清单：见本项目 `.workbuddy/memory/2026-09-13.md`
- 环境坑的完整记录：`docs_knowledge/开发过程中遇到的问题.md`（第 6、7、8 条）
- 路线图：`journal/ROADMAP.md`（注：P0/P1 各有一处已过期，待修）
