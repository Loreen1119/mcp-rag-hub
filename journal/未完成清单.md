# 未完成清单（可勾选）

> **怎么用**：做完一项就把 `- [ ]` 改成 `- [x]`，并把标题包进 `~~删除线~~`。
> **和 ROADMAP 的关系**：本文件是 `journal/ROADMAP.md`「三、未完成清单」的**可勾选版本**，
> 两者不一致时**以本文件为准**。ROADMAP 保留优先级理由与判断依据，本文件只管"还剩什么、怎么验"。
> 操作细节（命令、内存预算、避坑）见 `journal/端到端验收演练手册-2026-09-13.md`。
>
> **最后更新：2026-09-15 15:00** ｜ 总计 26 项 ｜ 已完成 17 项 ｜ **剩余 9 项**

## 进度概览

| 组 | 项数 | 已完成 | 剩余 | 说明 |
|---|---|---|---|---|
| A. 阻塞演示（P0） | 4 | **4** | **0** | ✅ **全组完成**（A1/A2/A3/A4）—— 结果见 `journal/端到端验收结果-2026-09-15.md`。⚠️ 仅 A2.4 的**界面层**待演示时确认 |
| B. 文档一致性 | 4 | **4** | **0** | ✅ 全组完成，文档口径已统一 |
| C. 功能缺口 | 10 | **7** | 3 | C6/C7/C8/C9/C10 已修；**C2 决策为"不做"**（数据不支持该投入） |
| D. 交付与包装 | 4 | 1 | 3 | D1 已完成 |
| E. 收尾与保洁 | 4 | 1 | 3 | |

> **剩余 9 项里，没有一项是"必须做"的**（2026-09-15 判断）：
> A/B 两组（决定"演得出、答得上"的）**已全部完成**；
> C1/C4/C5 是功能加分项，D2/D3/D4 是工程化包装，
> E2/E3/E4 是保洁与门面 —— 都属"有更好，没有也能讲完"。
> 唯一还"该验但没验"的是 **A2.4 的界面层**（服务不可用时界面是否正确显示降级，需起 Streamlit 看一眼）。

> ⚠️ **并发提醒（2026-09-14 晚）**：有两个会话同时在本仓库工作（一个只碰 `docs/`，
> 一个做 Docker/清单）。**各自 commit 必须用明确路径**（`git add <文件>`），
> **绝不要 `git add -A`** —— 否则会把对方未完成的改动一并提交，制造"message 与内容不符"的历史。
> 另外：提交前先 `git status` 看一眼，确认没有意外文件混进来。

> **切项原则**（吃过一次教训）：一项 = 一件有人真会去做的事，**按"文件/章节"切，不按"过期字符串"切**。
> 2026-09-14 改 `项目详解.md` 时，我先把"评测口径"和"界面章节"拆成 B2 / B5 两项，被指出这本来就是一件事 ——
> 已合并。同一篇文档要改对，就是一篇文章的活，别拆成两条。

---

## A. 阻塞演示（P0）—— 只差"跑"，不用写代码

- [x] ~~**A1 起 Ollama，并在新语料上跑通生成**~~ ✅ 2026-09-15 01:30
  3b 起服务后，18 条可答**全部产出答案**（生成率 100%），单条 14.5~43.8 秒。
  ⚠️ 顺带记一个坑：**启动脚本必须连 `ALL_PROXY` 一起清**（只清 `HTTP_PROXY`/`HTTPS_PROXY` 不够）——
  `llm_eval` 走 requests 正常，`agent` 走 ollama SDK（httpx）会全线 `connection_error`，
  表现得像"Ollama 挂了"。详见 `journal/端到端验收结果-2026-09-15.md` §7。

- [x] ~~**A2 四类端到端验收**~~ ✅ 2026-09-15（三类跑通，第四类被意外覆盖）
  完整结论、异常分析与面试口径：`journal/端到端验收结果-2026-09-15.md`

  - [x] ~~**A2.1 18 条可答 · 生成率 / 误拒率**~~ → **生成率 18/18 = 100%**
        （三维分数 Faith 0.70 / Relev 0.672 / Recall 0.478，但**裁判是 3b，仅供参考**）
  - [x] ~~**A2.2 10 条拒答 · 拒答率 / 误答率**~~ → **拒答率 90%（9/10）· 误答率 10%**
        其中 **near_miss 类 5/5 全对**。唯一失手 U08 是**状态误判不是幻觉** ——
        答案正文自己写了"未在文档中找到相关答案"，但 `status` 仍填 `answered`，validator 没识别这种模式。
  - [x] ~~**A2.3 3 条多证据 · 引用覆盖率**~~ → 作答率 66.7%、**跨文档引用率 0%**、引用覆盖 16.7%
        **关键数字：关键词覆盖 答案 50% vs 检索证据 83.3% → 瓶颈在生成层，不在检索层**
  - [ ] **A2.4 Ollama 不可用 · 降级路径** → **脚本层已被意外覆盖**：第一轮服务不可用时，
        13 条全部被正确标为"服务不可用 / 不计入分母"，**没有误报成业务拒答**（这正是设计要的效果）。
        界面层（Streamlit 显示"回答服务暂不可用 + 原文拼接"）待演示时确认。

- [x] ~~**A3 7b 裁判实测**~~ ✅ 2026-09-15 09:23（全量 18 条，约 63 分钟）
  **7b 确实比 3b 有区分度** —— 三维分数以 7b 版为准：

  | 指标 | 3b 裁判 | **7b 裁判** |
  |---|---|---|
  | Faithfulness | 0.7000（13/18 条同分） | **0.8556**（min 0.70） |
  | Answer Relevancy | 0.6722 | **0.7944** |
  | Context Recall | 0.4778 | **0.6000** |

  ⚠️ 两版评的是**同一批答案**（生成都用 3b，一字未改）—— 差值来自裁判能力，**不是答案变好**。
  ⚠️ 撞出一个缺陷，已记为 **C8**：`llm_eval.py:103` 的 `num_predict=512` 对 7b 太短 →
  JSON 被截断 → `_parse_score` **静默记 0 分**（S02 真实 0.9 被记成 0.0）。
  已做后处理恢复：`experiments/llm_evaluation_results.recovered.json`。
  ⚠️ 内存实测：7b 驻留占 **4,989 MB**（可用 7.34 → 2.54 GB），能跑完全量。

- [x] ~~**A4 延迟重测**~~ ✅ 2026-09-15 09:43（经 `scripts/reproduce_latency.py`）
  ⚠️ 标准入口 `experiments.py --quick` **跑不了**（rebuild 漏赋值 → 见 C9），故用脚本复现其测量逻辑
  （同 top_k、同计时点，改用"复用主库索引"绕开），脚本已入库。

  | 阶段 | Mean |
  |---|---|
  | BM25 检索 | 7.08 ms |
  | 向量检索 | 37.51 ms |
  | 图检索 | 11.13 ms |
  | RRF 融合 | 0.26 ms |
  | **CE 精排** | **33,973.76 ms** |
  | **合计** | **34,029.74 ms（约 34 秒/条）** |

  **结论：CE 占 99.8% 的延迟**，其余四项合计不到 60 ms。
  ⚠️ 旧值（395ms / CE 占 94%）**不可比** —— 旧语料仅 3 chunk + 英文 `ms-marco`，两个变量同时变了。
  ⚠️ 另发现：文档所称"CE 限制在 Top-20 候选内"**与代码不符** —— `src/fusion.py:122-123`
  对**全部候选**做前向，`top_k` 只控制输出条数。优化方向：RRF 之后先截断再送 CE。
  产物：`experiments/latency_profile.reproduced.json`

  **补充（13:17，C9 修复后）**：用**标准入口** `experiments.py --quick` 跑了**完整版**（44 分钟），
  五个实验全部产出新语料数据，`experiments/` 里的旧语料产物已全部替换。两件事：
  ① **延迟以标准入口为准：12.8 秒**（CE 12.76 秒 = 99.8%）。上午脚本的 34 秒是**机器有负载**下的数字 ——
  同一管线所有阶段全线慢 1.35~2.66 倍，**引用延迟时必须带上"机器空闲"这个前提**。
  ② **模块消融首次拿到新语料数据**，最重要的发现：**GraphRAG 没有任何增量** ——
  双路 0.7889 → 三路 0.7889（**ΔMRR = 0.0000**），Prec@5 还从 0.5444 降到 0.5333；
  而图检索在 `graph` 类别（图关联型问题）下 MRR 仅 **0.0833**，是全部配置里最低的。

---

## B. 文档一致性（8 个文件里还剩 7 个）

- [x] ~~**B1 `docs/技术视角详解.md`**~~ ✅ 09-14 已全篇重写
  （模型名 → `bge-small-zh-v1.5`/`bge-reranker-base`；切块 256/38 与"重叠 128 token"订正；
  CE 阈值 3.0 → 0.3 并补量纲说明；36 条 + train/test → 18 条 + 三套验收集 + 为何不拆分；
  指标表换成 09-12 新基线；延迟改为 395ms/94% 并标"待重测"；消融 2^4=16 组合 → 7 配置 × 18 条；
  "使用 Ragas 框架" → 自实现 + 现状（旧语料 3 条 3b 自评）；生成 Top-5 → Top-3；UI 四标签页 → 两 Tab）
- [x] ~~**B2 `docs/项目详解.md` 全篇对齐**~~ ✅ 09-14 已改完
  （评测章节：36 条 + train/test 拆分 → 18 道四类题 + 四级实测指标表 + 为何不再拆分；
  界面章节：从"四个标签页的分析面板"改为「问答模式 / 调试模式」两 Tab，
  补上答案 + 引用编号 + 证据卡片"已引用"标记，以及三种"没给出答案"的分类说明）
- [x] ~~**B3 章节 6 篇**~~ ✅ 09-15 已全部对齐
  ch02（MiniLM 窗口 → 512 上限 + 256 是**权衡**不是天花板）· ch03（MiniLM 选型 → bge-small-zh 512 维，
  补"英文模型对中文静默掉分"）· ch05（MiniLM 加载 → bge ~95MB；四标签页 → **两顶层 Tab + 调试模式四阶段**，
  补问答三态）· ch06（"3 Chunk 小语料" → 18 条新基线 + S04/S06；7b 评测口径 → **3b 生成 / 7b 裁判解耦**，
  旧 0.95 标注为历史，新增"生成层当前真实状态"表；四个评测脚本改名 + 新增 acceptance_eval）·
  ch08（CE 阈值 3.0 → **0.3** + 量纲陷阱 + "只用于改写门控"；7b 代码示例 → 3b 生成/7b 裁判 + 超时/追问）·
  ch10（ms-marco + 92% + "2 篇文档 3 Chunk" → 全部换成新数据；**电梯演讲里删掉 Faithfulness 0.95**，
  改为如实说"18 条 × 7b 裁判在跑"；速查清单数字全换）
  > 顺带发现并修掉两处 B2 遗漏：`项目详解.md` 仍写 384 维（→512）、
  > `docs/README.md` 的 ch05 描述仍是"四标签页"。
- [x] ~~**B4 `journal/ROADMAP.md` 自身 2 处过期**~~ ✅ 09-14 已修（P0 的"换模型"、P1 的 `_call_llm`）

> **B 组已全组完成**（09-15）：上面 4 项 + 已对齐不用再动的
> `面试背诵稿.md`、`resume-项目一.md`、`README.md`（指标表）、`ch09`、`开发过程中遇到的问题.md`。
> `docs/README.md` 里的"未对齐清单"已清空，改成了"已全部对齐"的说明。
> **文档里仍出现 `all-MiniLM` / `ms-marco` 的地方，都是在解释"为什么换掉它"，属刻意保留。**

---

## C. 功能缺口

- [ ] **C1 Phase 2A：文档管理**（3 个子项，是一整块活）
  - [ ] `app.py` 加上传入口（现在**没有** `file_uploader`，只有 sidebar）
  - [ ] 文档列表 / 删除
  - [ ] 上传后触发全量重建（真增量留给 C2）
- [x] ~~**C2 Phase 2B：真正的增量索引**~~ ⛔ **2026-09-15 决策：不做**（不是"没时间"，是数据不支持这个投入）
  - 实测 **1445 chunk 全量重建仅 77 秒**（`rebuild=True` 实跑），且只在导入时发生一次
  - 做真增量需引入文件级 manifest + chunk_id 稳定化，改生成规则会**强制升级 `CHUNK_ID_RULE_VERSION`
    → 反而触发一次全量重索引**；工程量半天以上
  - → **千级 chunk 规模下，增量索引是过度设计。等语料上到十万级再重新评估。**
  - **这个"不做的判断"本身就是面试材料** —— 完整的讲述口径见文末「关于 C2」。
- [x] ~~**C3 Vue 3 第二份语料**~~ ✅ 09-14 已落地（`corpora/vue-zh/`，110 篇 / 1804 chunk，CC BY 4.0）
- [ ] **C4 vue-zh 的索引与验收集**（09-14 落地语料**时新暴露**的一项，换语料必然要做）
  - [ ] 建索引：`set MCP_RAG_CORPUS=vue-zh` 后跑一次检索（首次自动重建）
  - [ ] 重写 18 条 golden + 10 条拒答 + 3 条多证据（旧三套钉死在 FastAPI 文件上）
  - [ ] 过一遍 `scripts/validate_golden.py`（可考证性，问题数须为 0）
  > 没有这一项，"支持多语料切换"只兑现了一半（语料在了，但查不了、评不了）
- [ ] **C5 MCP 服务联调证据**
  `src/mcp_server.py` 写好了，但 `tests/` 里**没有任何 mcp 测试**、`screenshots/` 里**没有 MCP 截图** ——
  "能被客户端调用"这件事从未被证明过。面试一问就空。
- [x] ~~**C6 `agent_eval.py` 阈值扫描区间沿用旧量纲**~~ ✅ 09-15 已修（09-14 对齐 `技术视角详解.md` 时发现）
  `calibrate_threshold` 扫的是 `1.0~6.0`（旧 `ms-marco` 无界 logits 的区间），而 `bge-reranker-base` 输出 [0,1]
  → **所有分数恒小于 1.0，每个阈值都判定"全部改写"，F1 恒定不变，校准静默空跑**（已用假数据复现：F1 恒 0.75）。
  **修法（比原计划更稳）**：不再硬编码区间，改为**按本次实测 CE 分数的 min/max 自适应扫描**（20 个点），
  换任何模型都不会再失效；顺带把打印精度从 `.1f` 提到 `.4f`（[0,1] 量纲下 `.1f` 会把扫描点显示重复）。
  复现验证：扫描区间 0.0010~0.9340，能正常扫出 F1=1.0 的最优点。
- [x] ~~**C7 评测脚本里的 Ollama 地址硬编码**~~ ✅ **2026-09-15 已修**（09-14 做 D1 容器化时发现）
  `llm_eval.py:98` 与 `agent_eval.py:61` 写死 `http://127.0.0.1:11434/api/chat` ——
  容器里 `127.0.0.1` 指容器自己，连不到宿主 Ollama（`agent.py` 走 SDK 本就读 `OLLAMA_HOST`，只有评测脚本不通）。
  **修法（比原计划的"两处各加 4 行"更彻底）**：
  ① `llm_eval` 新增 `_ollama_chat_url()`，**运行时**读 `OLLAMA_HOST`（默认 `127.0.0.1:11434`），
  兼容 `host.docker.internal:11434` 这种省略 scheme 的官方写法；
  ② `agent_eval` 的**重复实现直接删掉**，改为 `from llm_eval import _call_ollama`（与 `acceptance_eval` 一致）
  —— 那份复制品已经漂移了（`num_predict` 1536 vs 256、`timeout` 300 vs 120），
  **C8 修截断时只改了 llm_eval、agent_eval 仍是 256**，是"同一逻辑两处维护必漏一处"的实例证据。
  **验证**：**新增回归测试 `tests/test_ollama_host.py`（9 条，不联网）** —— 4 条地址解析
  （默认值 / 省略 scheme / 补斜杠去空白 / 每次调用重读而非常量）、1 条拦 `requests.post`
  断言实际 URL 来自 `OLLAMA_HOST`、1 条断言三个模块共用同一份实现；**pytest 53 passed**（原 44 + 新 9）。
  顺带修掉 pytest 报的 `llm_eval.py` docstring 无效转义（`D:\1software` → 加 `r"""`）。
  `docker-compose.yml` 与 `docs/reference/Docker部署说明.md` 的限制说明已同步为"已修"。
  > 测试过程中的两个环境坑（已写进测试注释 / `.gitignore`）：
  > ① 起真实 HTTP 服务的写法在本机会被注入的 `HTTP_PROXY` 劫持（ProxyError），
  > 清掉代理后又撞 Windows `ConnectionAbortedError(10053)`，且**单跑过、全量挂** → 改为拦 `requests.post`；
  > ② 全量 pytest 用默认 `%TEMP%` 会触发沙箱删除保护（需加 `--basetemp=.pytest_tmp`）。
- [x] ~~**C8 `llm_eval.py` 的 `num_predict=512` 会让 7b 评分被静默截断**~~ ✅ **2026-09-15 12:30 已修**
  `src/evaluation/llm_eval.py:103` 原写死 `num_predict: 512` → 7b 的 reason 更长 → 输出被截断 →
  `json.loads` 失败 → `_parse_score` **静默 `return 0.0`（不报错）**。
  实测 18 条里中招 1 条（S02 真实 0.9 被记成 0.0），Faithfulness 均值被拉低 0.05。
  **修法（两处）**：① `num_predict` 提到 **1536**；② `_parse_score` 解析失败时用正则从截断文本里
  抢救 `"score"`（reason 在 score 之后，故截断时 score 通常已完整）。
  验证：6 个用例全过（截断恢复 / reason 在前无法恢复时仍返回 0 / 越界裁剪 / markdown 包裹）。
- [x] ~~**C9 `VectorRetriever(rebuild=True)` 必然崩 —— 漏赋值**~~ ✅ **2026-09-15 12:30 已修**
  `src/retrievers.py:169` 的 `if rebuild:` 分支调用 `_rebuild_collection(...)` 却没把返回值
  赋给 `self.collection`（`else` 分支赋了），紧接着 logger 访问 `self.collection.name` → `AttributeError`。
  后果：**`experiments.py` 全部 6 处实验跑不了**（这解释了 `experiments/` 为何全是 7/27、8/04 旧数据）。
  **修法**：一行 `self.collection = self._rebuild_collection(...)`。
  验证：实跑 `rebuild=True` → 构造成功、`collection.name=exp__c9check`、`count()=1445`、耗时 77s。
  **主链路原本不受影响**（`app.py` / `agent` 用默认 `rebuild=False`）。
- [x] ~~**C10 `generate_report` 的结论文案是硬编码、会与数据矛盾**~~ ✅ **2026-09-15 14:55 已修**
  `experiments/report.md` 里原写着
  `[OK] 三路混合检索（+GraphRAG）MRR=0.7889，图检索为双路召回提供了增量价值。`
  —— 而实测 **双路 → 三路 ΔMRR = 0.0000**。
  修的过程中查出**两处与数据直接矛盾的论断**（不只是"文案硬编码"）：
  ① `if triple_mrr >= full_mrr:` —— **`>=` 在 Δ=0 时同样成立**，所以零增量也印"提供了增量价值"；
  ② `**BM25 在 exact_match 上的优势**` —— **直接断言、根本没检查是否真更大**，
     实测该类别 **BM25 与 Vector MRR 完全相同（均 0.8000）**；且结论只验证 semantic 一半。
  **修法**：新增 `_MIN_MEANINGFUL_GAIN = 0.001` + `_delta_phrase()`（中性表述），三处按实测 delta 分档。
  **新增回归测试 `tests/test_experiments_report.py`（12 条）** —— 核心是断言
  "**什么不许出现**"（如"Δ=0 时不得出现'带来了增量'/增量价值"、"持平时不得出现'BM25 的优势'"）。
  **`experiments/report.md` 已重新生成**（用现有三个产物直接调 `generate_report`，**没重跑实验**，省 44 分钟）。
  **顺带一个真发现**：设计四类时预期 exact_match 类"BM25 优势"，**实测被修正** ——
  在专有名词类上中文 embedding 已能靠字面相似度顶住；BM25 的价值在**逐条失败集合不重叠**，
  不在"这类题更强"。已补进 `ch06` 的"四个关键发现"。
  **`pytest` 65 passed。** commit `f78d713`、`44f6702`。

---

## D. 交付与包装（Phase 3）

- [x] ~~**D1 Dockerfile + docker-compose**~~ ✅ 09-14 已完成
  （`Dockerfile` + `.dockerignore` + `docker-compose.yml` + `docs/reference/Docker部署说明.md`；
  CPU 版 torch 单独装、模型不烧进镜像而挂宿主缓存、`HF_CACHE` 未设就 fail-fast、
  Streamlit 显式绑 `0.0.0.0`、健康检查用 `/_stcore/health`。
  **本机尚未 `build` 验证**——守护进程没起，且启动 Docker Desktop 会吃掉数 GB 内存，
  与"腾内存跑 7b"冲突；首次 build 注意事项写在部署文档里。
  顺带：容器化暴露了两处隐含假设，见新增的 **C7**。）
  - ⏳ **待办：`build` 验证**（用户 2026-09-14 23:30 决定推到 A 组跑完之后做，理由：不占内存、不与验收抢资源）
    恢复点：`docker compose build`（国内加速加 `--build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple`）
    → 然后按 `docs/reference/Docker部署说明.md` 第三节的六步验证。
    已确认可行：Docker Hub 直连超时，但 `daemon.json` 里的 `docker.1panel.live` 能取到基础镜像 manifest。
- [ ] **D2 FastAPI 服务层 + OpenAPI**（与 MCP 是两条路，别混为一谈）
- [ ] **D3 登录 / 权限**
- [ ] **D4 架构图 + 评测报告展示页 + 演示数据集**（README 现在只有 ASCII 流程图）

> D1/D2 是"工程化部署"这块短板的直接补丁，面试可讲性最高。

---

## E. 收尾与保洁

- [x] ~~**E1 收尾打包**~~ ✅ 09-14 已完成
  （`.gitignore` 让 `experiments/` 全部纳管 · 归档 v1 脚本到 `journal/` · 删除可重生的运行快照）
- [ ] **E2 KG 缓存重建**（**可选**）
  `data/knowledge_triples.jsonl` 不在（`kg_builder.py` 可重建）。`ENABLE_KG` 默认关闭，**不影响主链路**。
- [ ] **E3 Phase 0 产物在新语料上复核**（**可选**）
  `answerability_score_analysis.*` / `retrieval_miss_cases.json` / `multi_evidence_analysis.json`
  都是 9/11 旧语料产物。结论（"CE Top-1 分数无区分力"）不受语料影响，复核只是更严谨。
  - ✅ 09-14 已做**更该做的那一半**：新增 `experiments/README.md`，给该目录 14 个产物
    逐个标明「生成时间 / 生成脚本 / 语料归属 / 能不能引用」。起因是 `experiments/` 纳入版本控制后
    暴露出真问题 —— **整目录目前 100% 是旧语料产物**，却和即将落盘的新结果摆在同一层，
    而评测是**原地覆盖**同名文件的。先把坐标系立起来，比事后追溯划算。（commit `116fb98`）
  - 剩下的"真去新语料上重跑"仍是可选；且要跑检索，**属会产生噪音的活，并到 A 组一起跑**。
- [ ] **E4 UI 截图重截**
  现有 6 张是 8/25–8/26 的四阶段图，**不含** Phase 1B 之后的问答模式 / 拒答 / 降级三态。
  README 首页用的就是它们，属于门面问题。
  - ✅ 09-14 已做（同一项里的文案部分，非代码逻辑）：`app.py` 两处对外文案已换掉 ——
    占位符 `例如：RRF 如何工作？` → `例如：如何让某段耗时的处理在响应返回之后再执行？`
    （新值取自 golden 集 M06，已核实；旧值是旧语料概念，照着问会直接吃拒答）；
    caption `混合检索 + 证据门控 + 可追溯引用` → 去掉已废弃的"证据门控"，改为
    `混合检索 → RRF 融合 → 精排重排序 · 答案带 [n] 引用编号 · 证据不足会明确拒答`。
    commit `8dc2122`。**剩下的是重截三种状态的截图**，需要起 Streamlit。

---

## 建议的最短路径（只冲面试）

**A 组 + B 组已全部完成** —— 这两组是"演得出、答得上"的决定项：
A 决定了能演示（生成率 100% / 拒答率 90% / 引用覆盖 / 瓶颈定位），
B 决定了文档口径统一、不怕追问。**现在这个项目已经可以拿去讲了。**

剩余的都不阻塞面试，按性价比排（2026-09-15 判断）：

| 优先 | 事项 | 为什么 |
|---|---|---|
| ① | **A2.4 界面层确认**（起 Streamlit 看一眼降级路径） | 唯一"该验但没验"的。它验的是你项目的设计亮点：**服务挂了不能误报成业务拒答** —— 脚本层已过，界面层没看过 |
| ② | **E4 重截 UI 截图** | README 首页门面；和 ① 一起做最省事（同一次起服务） |
| ③ | **C1 上传入口** | 补"知识库"闭环。但要做对：**上传到独立语料目录**，别污染 `corpora/fastapi-zh`（那是带来源声明的同步语料） |
| ④ | D2 FastAPI 服务层 | 工程化短板最直接的一块 |
| — | ~~C2 增量索引~~ | **已判断不做**（见下） |

### 关于 C2：明确判断为「不做」，理由要留住

**不做，不是"没时间"，是数据不支持这个投入**：

- 实测 **1445 chunk 全量重建只要 77 秒**（`rebuild=True` 实跑），且只在导入时发生一次
- 而做真增量要引入文件级 manifest、改 chunk_id 生成规则（**会强制升级
  `CHUNK_ID_RULE_VERSION` → 反而触发一次全量重索引**），工程量半天以上
- → **在千级 chunk 规模下，增量索引是过度设计**

**这个"不做的判断"本身就是面试材料**：

> "考虑过做文件级增量索引。但实测 1445 个 chunk 全量重建只要 77 秒，是导入时的一次性开销；
> 做增量要引入 manifest 和 chunk_id 稳定化，改生成规则还会触发一次全量重索引 ——
> 复杂度上去了，收益在这个规模下几乎为零。所以我判断**在这个数据量下不做**，
> 等语料上到十万级再重新评估。"

这比"我做了增量索引"更能说明工程判断力（同 `journal/LightRAG源码笔记-2026-09-15.md` 里对 KG 升级方向的处置逻辑）。

---

## 历史留痕（已完成项）

- **09-12**：语料从项目自身 `docs/` 迁到 `corpora/fastapi-zh`（122 篇 / 1445 chunk）；
  三套验收集按新语料重写并通过可考证性校验；检索基线 CE MRR 0.7889 / Hit@5 100%；
  求职材料与文档基线对齐；ch09 重写 + 新增问题集；收敛为单一 golden set（删 train/test 拆分）
- **09-14**：新增 `corpora/vue-zh`（110 篇 / 1804 chunk，CC BY 4.0）；
  `experiments/` 纳入版本控制 + 归档历史脚本（E1）；新增拒答/多证据验收脚本
  （`acceptance_eval.py` + 9 条单测，四类验收入口补齐）；修正 ROADMAP 过期项（B4）；
  `项目详解.md` 全篇对齐（B2：评测口径 + 界面章节；界面章节一度被我误拆成 B5，已合并）；
  `技术视角详解.md` 全篇重写（B1，顺带发现 `agent_eval.py` 的 CE 阈值扫描区间仍是旧
  `ms-marco` 量纲 1.0~6.0 → 新增 C6）；
  `app.py` 两处对外过期文案修正（占位符换为 golden 集 M06 的真实问题、caption 去掉已废弃的
  "证据门控"）；新增 `experiments/README.md`（逐文件标明语料归属 —— 此前 15 个产物 **100% 是旧语料**的）；
  `journal/` 清理三份已被取代的旧产物（`PROGRESS.md` / `corpus_facts.md` / `PHASE0_RECOVERY.md`，
  判据：零引用 + 内容已被取代 + 删除零信息损失。当时 `EXECUTION_PLAN.md` 与 `kg_diag*.py` 有意保留；
  **09-15 已把 `EXECUTION_PLAN.md` 一并删除**，理由见下），
- **09-15**：**C6 修复**（`agent_eval.calibrate_threshold` 扫描区间由硬编码 `1.0~6.0` 改为
  **按实测分数 min/max 自适应**；已用假数据复现原 bug：所有 [0,1] 分数恒 <1.0 → F1 恒定，校准静默空跑）；
  **B3 完成**（ch02/03/05/06/08/10 六篇章节笔记全量对齐，B 组清零）。
  改 ch10 时发现旧稿让面试者背「Faithfulness 0.95」——那是旧语料旧评测集的产物，
  **已改成如实说"18 条 × 7b 裁判在跑"**。文档里仍出现旧模型名的地方都改成了解释"为什么换掉它"。

  **同日后续（A 组跑通 + 四个缺陷）**：
  - **A 组四项跑完**（01:30~13:17）：可答生成率 **100%**、拒答率 **90%**、7b 裁判三维分数
    （Faith 0.856 / Relev 0.794 / Recall 0.600）、延迟重测（标准入口 **12.79s**，CE 占 99.8%）。
  - **C8**：`num_predict=512` 对 7b 太短 → 评分 JSON 被截断 → **静默记 0 分**（已修，含正则抢救）。
  - **C9**：`retrievers.py` 的 `rebuild` 分支漏赋值 → **`experiments.py` 全部 6 处实验跑不了**
    （这解释了它为何长期只有 7/27、8/04 的旧数据）。一行修复后六类实验一次跑通。
  - **C7**：由另一会话完成（抽出 `_ollama_chat_url()` 运行时读 `OLLAMA_HOST`；
    并删掉 `agent_eval` 里那份**已漂移的重复实现** —— 它是"C8 只改了一处"的实例证据）。
  - **A4 完整版**（44 分钟）拿到首批新语料消融数据，**核心发现：GraphRAG 零增量**
    （双路 0.7889 → 三路 0.7889，ΔMRR = 0.0000；图路在 `graph` 类 MRR 仅 0.0833）。
  - **C10**：报告结论由硬编码改为数据驱动，查出两处与数据矛盾的论断；
    **顺带修正一个自己的预期** —— exact_match 类实测 BM25 与 Vector **持平**（均 0.8000），
    并非"BM25 优势"（ch06 已补记）。
  - `journal/` 清理第二轮：删 `EXECUTION_PLAN.md`；`lightrag-takeaways.md` 迁到 `docs/`
    并标注"改进方向已被消融数据否决"（把那句只有规划、没有实现的"面试话术"改写为正确讲法）。
