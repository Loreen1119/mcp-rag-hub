# 未完成清单（可勾选）

> **怎么用**：做完一项就把 `- [ ]` 改成 `- [x]`，并把标题包进 `~~删除线~~`。
> **和 ROADMAP 的关系**：本文件是 `journal/ROADMAP.md`「三、未完成清单」的**可勾选版本**，
> 两者不一致时**以本文件为准**。ROADMAP 保留优先级理由与判断依据，本文件只管"还剩什么、怎么验"。
> 操作细节（命令、内存预算、避坑）见 `journal/P0-演练手册.md`。
>
> **最后更新：2026-09-14 22:50** ｜ 总计 21 项 ｜ 已完成 3 项 ｜ **剩余 18 项**

## 进度概览

| 组 | 项数 | 已完成 | 剩余 | 说明 |
|---|---|---|---|---|
| A. 阻塞演示（P0） | 4 | 0 | **4** | 只差"跑"，入口已全部就绪 |
| B. 文档一致性 | 4 | 1 | 3 | 影响面试材料可信度 |
| C. 功能缺口 | 5 | 1 | 4 | |
| D. 交付与包装 | 4 | 0 | 4 | Phase 3 |
| E. 收尾与保洁 | 4 | 1 | 3 | |

---

## A. 阻塞演示（P0）—— 只差"跑"，不用写代码

- [ ] **A1 起 Ollama，并在新语料上跑通生成**
  ｜ ~15 分钟 ｜ 依赖：物理可用内存 ≳2.5 GB（3b）
  模型已装好（`qwen2.5:3b`），9/12 在**旧语料**上实测跑通过；换 `corpora/fastapi-zh` 后一次没跑。
  命令：独立窗口 `& "D:\1software\ollama\ollama-windows-amd64\ollama.exe" serve`
  （**别用 `nohup &`**、**别覆盖 `OLLAMA_MODELS`**）→ 然后 `python -m src.evaluation.llm_eval --quick`

- [ ] **A2 四类端到端验收**（入口已于 09-14 全部补齐，只差跑）

  - [ ] **A2.1 18 条可答 · 生成率 / 误拒率**
    `python -m src.evaluation.llm_eval` ｜ 20~40 分钟（3b 生成 + 3b 裁判）
    ⚠️ 跑前备份 `experiments/llm_evaluation_results.json`（现在已纳管，但仍会被覆盖）
  - [ ] **A2.2 10 条拒答 · 拒答率 / 误答率**
    `python -m src.evaluation.acceptance_eval --refusal` ｜ 3~8 分钟
  - [ ] **A2.3 3 条多证据 · 引用覆盖率**
    `python -m src.evaluation.acceptance_eval --multi` ｜ 2~4 分钟
  - [ ] **A2.4 Ollama 不可用 · 降级路径**
    停掉服务再问一句，看是否显示"回答服务暂不可用 + 原文拼接"（无需脚本）

- [ ] **A3 7b 裁判实测**（`acceptance_eval --judge` / `llm_eval` 的裁判升级）
  ｜ 依赖：物理可用 ≳6 GB ｜ 40~70 分钟
  `config.JUDGE_MODEL = qwen2.5:7b` 已配好但**从未跑过**。生成用 3b、裁判用 7b 是项目既定设计，
  缺这一环则"生成质量"的分数只有 3b 自评，不可信。

- [ ] **A4 延迟重测**
  `python -m src.evaluation.experiments --quick` ｜ 10~20 分钟
  `experiments/latency_profile.json` 还是 **7/27** 的值，基于英文 `ms-marco`；
  CE 已换成 `bge-reranker-base`（1.1GB），旧数字不可沿用（README/面试稿里已标"待重测"）。

---

## B. 文档一致性（8 个文件里还剩 7 个）

- [ ] **B1 `docs_knowledge/技术视角详解.md`** ｜ ~20 分钟
  9 处过期：`all-MiniLM-L6-v2`、`ms-marco-MiniLM-L-6-v2`、"36 条 + train/test"、CE 占 92% 等
- [ ] **B2 `docs_knowledge/项目详解.md`** ｜ ~5 分钟
  1 处："36 条评测集按 train/test 对半分离"
- [ ] **B3 章节 6 篇** ｜ ~40 分钟
  ch02（MiniLM 窗口）· ch03（MiniLM 选型）· ch05（MiniLM 加载）·
  ch06（"3 Chunk 小语料" + 7b 评测口径）· ch08（CE 阈值 3.0 + `qwen2.5:7b` 代码示例）·
  ch10（ms-marco + 92% + "2 篇文档 3 Chunk"）
- [x] ~~**B4 `journal/ROADMAP.md` 自身 2 处过期**~~ ✅ 09-14 已修（P0 的"换模型"、P1 的 `_call_llm`）

> 已对齐、不用再动的：`面试背诵稿.md`、`resume-项目一.md`、`README.md`（指标表）、
> `ch09`、`docs_knowledge/README.md`（警示）、`开发过程中遇到的问题.md`。
> `docs_knowledge/README.md` 第 60 行列的就是这份"未对齐清单"，改完一处就删一处。

---

## C. 功能缺口

- [ ] **C1 Phase 2A：文档管理**（3 个子项，是一整块活）
  - [ ] `app.py` 加上传入口（现在**没有** `file_uploader`，只有 sidebar）
  - [ ] 文档列表 / 删除
  - [ ] 上传后触发全量重建（真增量留给 C2）
- [ ] **C2 Phase 2B：真正的增量索引**（3 个子项）
  - [ ] 文件级 manifest（现在只到 corpus-hash 语料集级，任何文档一改就整体重建）
  - [ ] chunk_id 稳定化
  - [ ] 向量库按文档删除 / 新增
- [x] ~~**C3 Vue 3 第二份语料**~~ ✅ 09-14 已落地（`corpora/vue-zh/`，110 篇 / 1804 chunk，CC BY 4.0）
- [ ] **C4 vue-zh 的索引与验收集**（09-14 落地语料**时新暴露**的一项，换语料必然要做）
  - [ ] 建索引：`set MCP_RAG_CORPUS=vue-zh` 后跑一次检索（首次自动重建）
  - [ ] 重写 18 条 golden + 10 条拒答 + 3 条多证据（旧三套钉死在 FastAPI 文件上）
  - [ ] 过一遍 `scripts/validate_golden.py`（可考证性，问题数须为 0）
  > 没有这一项，"支持多语料切换"只兑现了一半（语料在了，但查不了、评不了）
- [ ] **C5 MCP 服务联调证据**
  `src/mcp_server.py` 写好了，但 `tests/` 里**没有任何 mcp 测试**、`screenshots/` 里**没有 MCP 截图** ——
  "能被客户端调用"这件事从未被证明过。面试一问就空。

---

## D. 交付与包装（Phase 3）

- [ ] **D1 Dockerfile + docker-compose**（项目根**无**任何 Docker 相关文件）
- [ ] **D2 FastAPI 服务层 + OpenAPI**（与 MCP 是两条路，别混为一谈）
- [ ] **D3 登录 / 权限**
- [ ] **D4 架构图 + 评测报告展示页 + 演示数据集**（README 现在只有 ASCII 流程图）

> D1/D2 是"工程化部署"这块短板的直接补丁，面试可讲性最高。

---

## E. 收尾与保洁

- [x] ~~**E1 收尾打包**~~ ✅ 09-14 已完成
  （`.gitignore` 让 `experiments/` 全部纳管 · 归档 v1 脚本到 `journal/` · 删除可重生的运行快照）
- [ ] **E2 KG 缓存重建**（**可选**）
  `data/knowledge_triples.jsonl` 不在（`kg_builder.py` 可重建）。`ENABLE_KG` 默认关闭，**不影响主链路**。
- [ ] **E3 Phase 0 产物在新语料上复核**（**可选**）
  `answerability_score_analysis.*` / `retrieval_miss_cases.json` / `multi_evidence_analysis.json`
  都是 9/11 旧语料产物。结论（"CE Top-1 分数无区分力"）不受语料影响，复核只是更严谨。
- [ ] **E4 UI 截图重截**
  现有 6 张是 8/25–8/26 的四阶段图，**不含** Phase 1B 之后的问答模式 / 拒答 / 降级三态。
  README 首页用的就是它们，属于门面问题。

---

## 建议的最短路径（只冲面试）

只要 **A 组（4 项）+ B 组（3 项）+ E4** 共 **8 项**，这个项目就能"演得出、答得上"：
A 决定能不能演示，B 决定敢不敢让人追问代码，E4 是门面。
C/D 是加分项，E2/E3 是保洁。

**下一步最该做的仍然是 A1 → A2**：唯一能产出"答得对"类数字的地方。
现在项目所有指标都在证明"检索准"，还没有一条证明"回答质量好"。

---

## 历史留痕（已完成项）

- **09-12**：语料从项目自身 `docs/` 迁到 `corpora/fastapi-zh`（122 篇 / 1445 chunk）；
  三套验收集按新语料重写并通过可考证性校验；检索基线 CE MRR 0.7889 / Hit@5 100%；
  求职材料与文档基线对齐；ch09 重写 + 新增问题集；收敛为单一 golden set（删 train/test 拆分）
- **09-14**：新增 `corpora/vue-zh`（110 篇 / 1804 chunk，CC BY 4.0）；
  `experiments/` 纳入版本控制 + 归档历史脚本（E1）；新增拒答/多证据验收脚本
  （`acceptance_eval.py` + 9 条单测，四类验收入口补齐）；修正 ROADMAP 过期项（B4）
